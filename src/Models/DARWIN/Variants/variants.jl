"""DARWIN variant recipes.

Each file in this folder registers one or more `Agate.Models` variants.

Keep these files small: they should mostly wire together existing tracers,
dynamics builders, and parameter overrides.
"""

include("citation2026_a.jl")
