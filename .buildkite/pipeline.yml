steps:
  - label: "GPU smoke test"
    agents:
      queue: "GPU"
    commands:
      - nvidia-smi
      - julia --version
      - julia -e 'using Pkg; Pkg.add("CUDA"); using CUDA; CUDA.versioninfo()'